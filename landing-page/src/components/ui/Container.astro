---
interface Props {
  class?: string;
}

const { class: className = '' } = Astro.props;
---

<div class:list={['mx-auto w-full max-w-container-max px-margin-desktop', className]}>
  <slot />
</div>
