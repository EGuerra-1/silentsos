# Archivo .env.example para SilentSOS

# --- API ---
API_CONTAINER_NAME=api_ejemplo
API_PORT=4000

# --- Base de Datos ---
# DB_HOST=db dentro de Docker | 127.0.0.1 desde tu máquina local
DB_CONTAINER_NAME=db_ejemplo
DB_HOST=127.0.0.1
DB_USER=usuario_demo
DB_PASSWORD=ClaveDemo123
DB_NAME=database_demo
DB_INTERNAL_PORT=5432
DB_EXTERNAL_PORT=5435
DB_PORT=5435

# --- Entorno ---
NODE_ENV=development

# --- JWT ---
JWT_SECRET_ADMIN=AdminSecretExample_123456789
JWT_SECRET_USER=UserSecretExample_987654321
JWT_EXPIRES_IN_ADMIN=30d
JWT_EXPIRES_IN_USER=30d

# --- CORS ---
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://example.com

# --- OpenAI (triage visual con 2 imágenes) ---
OPENAI_API_KEY=sk-proj-your_openai_key_here
OPENAI_MODEL=gpt-4o
OPENAI_TRIAGE_TIMEOUT_MS=15000

# --- ElevenLabs (TTS para llamada + STT para operador en modo interactivo) ---
ELEVENLABS_API_KEY=your_elevenlabs_api_key_here
ELEVENLABS_VOICE_ID=your_voice_id_here
ELEVENLABS_TTS_MODEL=eleven_turbo_v2_5
ELEVENLABS_STT_MODEL=scribe_v2

# --- Zavu (WhatsApp a contactos de emergencia vía n8n) ---
ZAVU_API_KEY=zv_live_your_zavu_key_here
ZAVU_SENDER_ID=your_sender_id_here
ZAVU_DEFAULT_CHANNEL=whatsapp

# --- n8n (workflow SilentSOS Emergency SMS via Zavu) ---
N8N_EMERGENCY_WEBHOOK_URL=https://your-n8n-domain/webhook/silentsos-emergency-sms
N8N_MEDICATION_WEBHOOK_URL=https://your-n8n-domain/webhook/silentsos-medication-taken
N8N_EMERGENCY_WEBHOOK_SECRET=
N8N_WEBHOOK_TIMEOUT_MS=10000

# --- Twilio (llamadas de voz al 911/número de demo) ---
TWILIO_ACCOUNT_SID=ACyour_account_sid_here
TWILIO_AUTH_TOKEN=your_twilio_auth_token_here
TWILIO_PHONE_NUMBER=+1XXXXXXXXXX
# Reintentos automáticos si la llamada no se contesta o falla (no-answer/busy/failed).
MAX_CALL_ATTEMPTS=2
CALL_RETRY_DELAY_MS=20000

# --- Número que recibirá las llamadas de emergencia ---
EMERGENCY_PHONE_NUMBER=+503XXXXXXXX

# --- URL pública del servidor (para TwiML, Media Streams y OpenAI Vision) ---
# Twilio y OpenAI Vision necesitan acceso público a los archivos generados.
# En desarrollo usa ngrok: ngrok http 4000
PUBLIC_BASE_URL=https://your-ngrok-or-domain.com

# --- Almacenamiento local de imágenes y audio (no se usa AWS S3) ---
LOCAL_STORAGE_PATH=storage
